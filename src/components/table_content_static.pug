mixin table()
  -var i=1
  table(class="table table-striped")
    thead
      tr
        th(scope="col") #
        th(scope="col") First Name
        th(scope="col") Last Name
        th(scope="col") Handle
    tbody
      tr
        th(scope="row") #{i++}
        th Test
        th McTest
        th @testMctest
      tr
        th(scope="row") #{i++}
        th Tester
        th Testerson
        th @liltesty
      tr
        th(scope="row") #{i++}
        th Testy
        th Mctestface
        th @mctestface
      tr
        th(scope="row") #{i++}
        th Test
        th TripleTestson
        th @ttt